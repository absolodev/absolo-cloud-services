# design-tokens

CSS variables and theme tokens shared across frontends.

Plan reference: `../docs/plans/04`- (search docs/plans/ for the matching `04-*-d1e00e.md` file).

Status: scaffold (no source yet).
