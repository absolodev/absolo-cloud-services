/**
 * Toast bridge over `sonner`.
 *
 * `Toaster` is mounted once near the app root; consumers call `toast.success(…)`,
 * `toast.error(…)`, etc. anywhere in the tree.
 */
export { Toaster, toast } from 'sonner';
