export function OrgsPage() {
  return (
    <div className="mx-auto max-w-6xl px-8 py-8">
      <header className="mb-6">
        <h1 className="font-mono text-2xl font-semibold tracking-tight">organisations</h1>
        <p className="mt-1 font-mono text-xs text-fg-muted">
          search, suspend, impersonate (audited)
        </p>
      </header>
      <div className="rounded-lg border border-dashed border-border/80 p-12 text-center font-mono text-xs text-fg-muted">
        org admin endpoints not yet wired \u2014 see plan 04 (iam)
      </div>
    </div>
  );
}
