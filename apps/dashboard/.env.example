# Dashboard local-dev defaults. Copy to `.env.local`.
#
# Vite picks up VITE_* env vars automatically. Values here are forwarded by
# the dev server to the client bundle.

# Where the SDK should hit. Leave blank to use window.location.origin (Vite
# proxy will forward /v1/* to the control plane during dev).
VITE_API_BASE_URL=
