# host-agent

Per-host Rust agent: k3s lifecycle, telemetry, Web SSH proxy.

Plan reference: `../docs/plans/10`- (search docs/plans/ for the matching `10-*-d1e00e.md` file).

Status: scaffold (no source yet).
