# web-ssh-gateway

WebSocket <-> host-agent exec bridge for Web SSH.

Plan reference: `../docs/plans/20`- (search docs/plans/ for the matching `20-*-d1e00e.md` file).

Status: scaffold (no source yet).
