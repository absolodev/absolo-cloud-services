# edge-proxy

Pingora-based public ingress: TLS, routing, rate-limit, WAF.

Plan reference: `../docs/plans/11`- (search docs/plans/ for the matching `11-*-d1e00e.md` file).

Status: scaffold (no source yet).
